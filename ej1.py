def ordenar_lista(lista):
  lista.sort(reverse=True)
  return lista


def_ordenarlista_menor_mayor(lista):
  lista.sort()
  return lista

def_ordenar_lista_pares_primero(lista):
  listasort(reverse=True)
  lista_pares=[]
  lista_impares=[]
  for num in lista:
    if num % 2==0
    lista_pares.append(num)
  else:
   lista_impreas.append(num)
   return lista_pares + lista_impares

def oredenar_lista_paresu(lista):
  lista.sort(reverse=True)
  lista_pares=[]
  lista_impares=[]
  for num in lista:
   if num % 2==0
    lista_pares.append(num)
  else:
    lista_impreas.append(num)